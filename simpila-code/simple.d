class Complex {
    var float Real;
    var float Imag;
}

proc Swap( ref int a, ref int b )
{
    var int tmp;
    tmp := a; 
    a := b;
    b := tmp;
   
    if a > b then
    {
      while temp do {
        mordi := mann;
      }
    }
    
    printfloat( Square( num ) );

	return tmp;
}
